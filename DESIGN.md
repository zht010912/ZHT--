# ActionFlow 前端设计系统

## 产品场景

ActionFlow 面向需要把会议讨论转成明确待办的项目负责人。页面采用工作台模式：用户先看会议摘要、关键结论和待办，需要核对时再进入原始记录或审核记录。

## 设计观点

**Meeting Workspace｜会议纪要工作台**

会议纪要的价值不在于写得长，而在于让人快速知道“说了什么、定了什么、接下来谁去做”。详情区参考腾讯会议纪要的内容顺序，把会议概览、关键结论和待办放在默认页；原始记录与审核记录各自独立，避免同一内容左右重复。供应商和具体模型属于实现与审计信息，不在工作台界面展示；用户只需要理解“生成、核对、采用、跟进”。

参考资料：

- [腾讯会议：元宝纪要（原 AI 纪要）](https://meeting.tencent.com/support/topic/2221/)
- [腾讯会议：新版智能录制](https://meeting.tencent.com/support/topic/2228/index.html)

高辨识度元素是 **Evidence Spine｜证据脊**：

```text
1 · 记录入档 → 2 · 生成纪要 → 3 · 人工确认 → 4 · 同步待办
```

节点状态随当前会议真实数据变化。珊瑚色表示需要人作出判断，翡翠色表示已经进入正式协同流程。

## 颜色角色

| 角色 | 色值 | 用途 |
|---|---:|---|
| Carbon Rail | `#151D2A` | 控制轨与强层级 |
| Archive Paper | `#FFFFFF` | 主内容纸面 |
| Field Grey | `#F2F5F8` | 页面工作区背景 |
| Evidence Blue | `#1769E0` | 原文证据、选中、主操作 |
| Review Coral | `#D04A3B` | 待人工审签、安全提醒 |
| Approval Jade | `#07856B` | 已确认、已加入待办、服务正常 |

正文色与纸面背景对比度不低于 4.5:1；状态不能只依靠颜色，必须同时保留文本。

## 排版

- 标题与数字：`Bahnschrift`，中文回退到 `Microsoft YaHei UI`。
- 正文与表单：`Microsoft YaHei UI / PingFang SC / Noto Sans CJK SC`。
- 证据编号与数据标签：`Cascadia Code / Consolas`，仅用于真实编号。
- 正文默认 14px；重要内容 15–17px；审核记录正文不低于 11px，且保持足够对比度。

## 布局

- 桌面：88px 控制轨 + 会议列表 + 当前会议工作台。
- 1180px 以下：控制轨收窄，历史会议与当前会议仍保持双栏。
- 800px 以下：控制轨转为顶部导航，历史会议与详情纵向排列。
- 540px 以下：证据脊改为 2×2，行动项与审核按钮转为单列，弹窗内部可滚动。

## 组件语言

- 大容器最多使用 12px 圆角；内容依靠规则线、浅色底和留白分组。
- 图标统一为 1.7px 描边的内联 SVG，不使用 Unicode 符号或 emoji 代替图标。
- 主按钮使用证据蓝；生成会议纪要使用碳黑；确认状态使用翡翠；待确认状态使用珊瑚。
- 阴影只承担工作区层级，不用大面积渐变或装饰抢占信息注意力。

## 用户任务与交互

| 用户要完成的事 | 工作台入口 | 完成反馈 |
|---|---|---|
| 找到一场历史会议 | 左侧搜索、类型和待办状态筛选 | 选中会议保持高亮，右侧立即更新 |
| 快速了解会议结果 | 默认“会议纪要”页 | 显示摘要、关键结论、待办和确认状态 |
| 核对纪要是否有依据 | “查看原文依据”或“原始记录”页 | 原文与整理内容分开显示，不混淆来源 |
| 决定是否采用初稿 | “审核记录”页 | 可采用、修改后采用或不采用；结果文案与按钮一致 |
| 推进会后工作 | “待办与跟进”区 | 可添加、编辑、完成或恢复待处理 |
| 生成服务暂时不可用 | 失败卡片与手动入口 | 说明原因，同时保留会议和手动待办功能 |

## 动效与可访问性

- 一次有目的的动效：会议切换时，当前证据节点完成“审签落章”动画。
- `prefers-reduced-motion: reduce` 下关闭非必要动画。
- 所有按钮、输入和可选择会议均有明显 `:focus-visible`。
- 支持跳到主要内容、原始记录键盘滚动、Dialog 标题关联、Toast 成功/错误语义以及关闭后焦点返回。

## 文案原则

- 控件直接描述结果，例如“生成会议纪要”“采用初稿”“保存修改并采用”。
- 明确区分“纪要初稿”和“已确认待办”，不暴露供应商、模型名或实现参数。
- 错误需说明问题与恢复方向；空状态需告诉用户下一步能做什么。
