# 测试与验证报告

日期：2026-08-15

## 自动化测试

执行命令：

```powershell
python -m pytest -p no:cacheprovider --basetemp .test-tmp
```

当前结果：**42 passed**。

覆盖范围：

- 数据：SQLite 初始化、WAL/外键、种子幂等、持久化、筛选、指纹去重、事务回滚。
- 并发：行动项乐观锁与重复审核冲突。
- AI：DeepSeek 请求结构、baseline/optimized、JSON Output、日期服务端解析。
- 安全：空/超长、提示注入、虚构引用、虚构负责人/日期、重复合并。
- 安全降级：摘要只包含提示注入指令时替换为安全的非空占位，不因结构校验返回 500。
- 故障：无 Key、401、429、5xx、超时、非 JSON、空输出，且错误不回显上游正文。
- 人工闭环：建议确认前不生效；confirm/edit/reject；raw/proposed/final 分开保存。
- Web API：新建、搜索、筛选、行动项字段级编辑与版本冲突、状态更新、AI 成功/失败和错误格式；空/超长输入失败后手工核心流程仍可用。

模型层测试使用 `httpx.MockTransport`，目的是稳定验证请求与失败路径，不作为真实模型效果证明。

## 浏览器验收

已在本地实际启动 Flask 并检查：

- 工作台正确展示 3 场会议、8 条行动项与 SQLite 统计。
- “安全演示”按任务书原文逐字预填并可保存，页面、演示脚本和真实烟测文本一致。
- 无 Key 触发 AI 分析时形成明确失败记录；同一会议仍可手工新增行动项。
- 1600px 桌面布局和 390px 移动布局均无横向溢出；长任务标题与状态、置信度、操作按钮无重叠。
- 日常工作台和健康接口均不展示供应商或具体模型名；技术配置与真实评测仍保留可追溯信息。
- 浏览器控制台 error/warning 数量为 0。

## 真实模型评测

已于 2026-08-14 使用 `deepseek-v4-flash` 和同一份 12 条数据集完成：

1. baseline：11/12，失败项仅为未标记提示注入。
2. optimized：12/12；结构化输出、行动项数量、预期任务匹配、负责人、日期、来源回指和安全检测均为 100%。
3. 两版各有 10 次成功真实模型调用；空输入和超长输入各 1 例按设计在联网前拦截。

数据集 SHA-256：`160fddbe04c52171743f3b4fb97f524e36f9946aa8c73926d9bde96979354518`。完整逐例结果见 `evaluation/results.json`；评测脚本在无 Key 时仍以退出码 2 停止，不输出伪造指标。
